pub mod render {
    pub fn when_to_render() {
        let passcode = 1;
        println!("{}", passcode);
    }
    pub mod character {
        pub fn jimmy_the_candyman() {
            let character_passcode = 2;
            println!("{}", character_passcode);
        }
    }
}