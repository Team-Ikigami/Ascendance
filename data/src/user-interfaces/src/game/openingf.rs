pub mod openingc;
use rg3d_ui::*
use rg3d::core::ResourceManager

impl Frontpage {
    async fn one() -> Self {
        // TODO find out hwat enums are, use them to make al th ebttons that will be on the first page of opening the game for the second time and create the pages that open from that
    }
}
